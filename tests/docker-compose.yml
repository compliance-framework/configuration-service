services:
  configuration-service:
    build:
      context: ../
      dockerfile: tests/configuration-service/Dockerfile
    ports:
      - "8080:8080"
      - "2345:2345"
    security_opt:
      - seccomp:unconfined
